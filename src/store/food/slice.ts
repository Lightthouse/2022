import {createSlice} from "@reduxjs/toolkit";


export const slice = createSlice({
    name: "food",
    initialState: {},
    reducers: {
        get(state, action) {},
        update(state, action) {},
        delete(state, action) {},
    }
})